apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: journal-dragonfly-sessions
spec:
  replicas: 2
  args:
  - "--proactor_threads=2"
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 640Mi
  snapshot:
    cron: "* * * * *"
    persistentVolumeClaimSpec:
      storageClassName: journal-gp3
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 2Gi
