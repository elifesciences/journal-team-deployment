apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: journal

resources:
- cache.yaml
- sessions.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/component: dragonfly-cache
    app.kubernetes.io/instance: ${instance_name:-journal}-${env}
configurations:
- labelsconfig.yaml
