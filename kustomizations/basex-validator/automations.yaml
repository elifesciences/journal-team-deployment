apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: basex-validator
spec:
  interval: 5m
  image: ghcr.io/elifesciences/basex-validator
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: basex-validator
spec:
  imageRepositoryRef:
    name: basex-validator
  filterTags:
    pattern: '^(?P<semver>[0-9]+\.[0-9]+\.[0-9]+).*'
    extract: '$semver'
  policy:
    semver:
      range: '*'
