apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: epp-client-stable
spec:
  imageRepositoryRef:
    name: epp-client
  filterTags:
    pattern: '^main-[a-fA-F0-9]+-(?P<ts>.*)'
    extract: '$ts'
  policy:
    numerical:
      order: asc
