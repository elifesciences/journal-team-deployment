apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: journal-eligibility-role
  namespace: journal--test
spec:
  interval: 1m0s
  sourceRef:
    kind: GitRepository
    name: journal-team-deployment
    namespace: flux-system
  path: ./kustomizations/journal/eligibility-role
  prune: true
  targetNamespace: journal--test
  postBuild:
    substitute:
      # deployment vars
      env: test
      aws_oidc_arn: "${cluster_oidc_arn}"
      aws_oidc_url: "${cluster_oidc_url}"
      irsa_namespace: journal--test
      eligibility_json_s3_object_arn: arn:aws:s3:::elife-eligibility-tool/eligibility.json
