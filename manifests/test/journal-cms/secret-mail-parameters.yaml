apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: journal-cms-mail-parameters
  namespace: journal--test
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: secret-store
    kind: ClusterSecretStore
  target:
    name: journal-secret-parameters
    creationPolicy: Owner
    template:
      data:
        ssmtp.conf: |
          root=it-admin@elifesciences.org
          hostname=localhost
          mailhub={{ .mailer_host | toString }}
          UseTLS=YES
          UseSTARTTLS=YES
          AuthUser={{ .mailer_username | toString }}
          AuthPass={{ .mailer_password | toString }}
  dataFrom:
  - extract:
      key: journal-team/journal-cms
