---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: journal-cms-app-secret
  namespace: journal--test
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: secret-store
    kind: ClusterSecretStore
  target:
    name: journal-cms-app-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: journal-team/journal-cms
