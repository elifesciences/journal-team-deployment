apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: journal-preview-pr-dragonfly
  namespace: journal--previews
spec:
  interval: 1m0s
  sourceRef:
    kind: GitRepository
    name: journal-team-deployment
    namespace: flux-system
  path: ./kustomizations/journal/dragonfly
  prune: true
  targetNamespace: journal--previews
  # avoid name collision with journal app
  namePrefix: preview-
  postBuild:
    substitute:
      env: test
