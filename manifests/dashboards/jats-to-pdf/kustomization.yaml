apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: journal
configMapGenerator:
- name: jats-to-pdf-dashboards
  files:
  - prod-dashboard.json
  options:
    labels:
      grafana_dashboard: "1"
    annotations:
      kustomize.toolkit.fluxcd.io/substitute: disabled
